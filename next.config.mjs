/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['http://Thecreatorshub.in'], // if you're loading external images
  },
};

module.exports = nextConfig;
